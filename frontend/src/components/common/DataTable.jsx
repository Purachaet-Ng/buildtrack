/**
 * The shared TanStack Table wrapper: sorting, pagination, empty state, and the
 * loading skeleton.
 *
 * IMPORTANT (PLAN.md §6.1): the card-list fallback below `md:` is solved HERE,
 * once, so every list page inherits it. Do not write a per-page mobile table.
 * Takes a `renderCard(row)` prop for what each row becomes on a narrow screen.
 *
 * Sprint 3. Install @tanstack/react-table then.
 */

export function DataTable() {
  return null;
}
