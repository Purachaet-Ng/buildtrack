/**
 * Page title (24px/600), optional breadcrumb, and a right-aligned action slot.
 * Used at the top of every list and detail page.
 * Sprint 3.
 */

export function PageHeader() {
  return null;
}
