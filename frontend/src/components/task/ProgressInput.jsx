/**
 * 0–100 slider, step 5, sage fill. The engineer's one recurring action, so this
 * is a MOBILE-FIRST surface (PLAN.md §6.1) — 44px minimum tap target, usable
 * with gloves on.
 *
 * Value is clamped 0–100 client-side and re-validated by the API's Zod schema.
 * Sprint 4.
 */

export function ProgressInput() {
  return null;
}
