/**
 * Task detail dialog (600px): name, status + priority chips, description,
 * assignee, dates, ProgressInput, attachments, and the comment thread.
 *
 * CLIENT sees an "อนุมัติงาน" primary button when the task is in REVIEW.
 * Sprint 4 (shell) / Sprint 5 (comments).
 */

export function TaskDialog() {
  return null;
}
