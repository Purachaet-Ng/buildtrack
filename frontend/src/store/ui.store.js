/**
 * Zustand: local UI state only — sidebar drawer open/closed, the Kanban vs list
 * view toggle, and (Phase 2) theme.
 *
 * Nothing here comes from the server.
 * Sprint 2.
 */

export const useUiStore = null;
