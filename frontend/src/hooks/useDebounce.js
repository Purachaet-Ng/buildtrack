/**
 * Debounces a value — used by every search input so typing does not fire a
 * request per keystroke. 300ms default.
 * Sprint 3.
 */

export function useDebounce() {
  throw new Error("useDebounce: not implemented (Sprint 3)");
}
