/**
 * ค่าใช้จ่าย: budget summary, category breakdown, expense table with a pinned totals row. Unreachable for STAFF — renders the 403 page. The backend also returns 403 on every /expenses endpoint.
 * Sprint 6.
 */

export function ExpensesPage() {
  return null;
}
