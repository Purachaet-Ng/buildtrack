/**
 * เอกสาร: grid/list toggle, docType filter, upload dialog with per-file progress. Delete offered only to ADMIN, PM, and the uploader.
 * Sprint 6.
 */

export function DocumentsPage() {
  return null;
}
