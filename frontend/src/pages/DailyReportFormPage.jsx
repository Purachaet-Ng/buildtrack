/**
 * Daily report create/edit. MOBILE-FIRST (PLAN.md 6.1) — designed at 375px, 44px tap targets, sticky footer. A route, not a dialog. Handles the 409 duplicate with a link to edit the existing report.
 * Sprint 5.
 */

export function DailyReportFormPage() {
  return null;
}
